<!DOCTYPE hibernate-configuration PUBLIC
    "-//Hibernate/Hibernate Configuration DTD 3.0//EN"
    "http://hibernate.sourceforge.net/hibernate-configuration-3.0.dtd">
<hibernate-configuration>
    <session-factory>
        <!-- Configuración de la conexión a la base de datos -->
        <property name="hibernate.connection.driver_class">com.mysql.cj.jdbc.Driver</property>
        <property name="hibernate.connection.url">jdbc:mysql://localhost:3306/ferremax_db</property>
        <property name="hibernate.connection.username">root</property>
        <property name="hibernate.connection.password"></property> <!-- Agrega tu contraseña aquí si es necesaria -->

        <!-- Dialecto de Hibernate para MySQL -->
        <property name="hibernate.dialect">org.hibernate.dialect.MySQL8Dialect</property>

        <!-- Mostrar las consultas SQL en la consola (opcional) -->
        <property name="hibernate.show_sql">true</property>

        <!-- Actualizar la base de datos al iniciar (solo en desarrollo) -->
        <property name="hibernate.hbm2ddl.auto">update</property>

        <!-- Mapeo de las clases de entidad -->
        <mapping class="com.mycompany.hibernateferremax.Producto"/> 
    </session-factory>
</hibernate-configuration>
